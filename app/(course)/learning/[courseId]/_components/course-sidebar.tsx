interface CourseSidebarProps {
  course: any;
  progressCount: number;
  // label: string;
  // id: string;
  // isCompleted: boolean;
  // courseId: string;
  // isLocked: boolean;
}

export const CourseSidebar = ({
  course,
  progressCount,
}: CourseSidebarProps) => {
  return <div>Course Sidebar</div>;
};
